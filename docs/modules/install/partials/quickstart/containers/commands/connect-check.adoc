// Check slurmrestd connectivity. Include inside Docker/Podman tabs.

ifeval::["{quickstart}" == "containers"]
[source,console,subs=attributes]
----
$ {runtime} run --rm \
    {podman-userns} {container-user} \
    -v {config-dir}/agent.ini:/etc/slurm-web/agent.ini:ro \
    {secrets-volume} \
    ghcr.io/rackslab/slurm-web-agent:latest connect-check
✅ connection successful! (cluster: hpc, slurm: 25.11.0, api: 0.0.44)
----
endif::[]

ifeval::["{quickstart}" == "compose"]
[source,console,subs=attributes]
----
$ {compose-env} {compose-cmd} run --rm agent connect-check
✅ connection successful! (cluster: hpc, slurm: 25.11.0, api: 0.0.44)
----
endif::[]
