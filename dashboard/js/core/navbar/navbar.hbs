<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button"
              class="navbar-toggle collapsed"
              data-toggle="collapse"
              data-target="#navbar"
              aria-expanded="false"
              aria-controls="navbar">
        <span class="sr-only">
          Toggle navigation
        </span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <span class="navbar-brand" id="brand-name">
        {{#if (isTrue clusterName)}}
          <span class="cluster">{{clusterName}}</span>'s Slurm HPC Dashboard
        {{else}}
          Slurm HPC Dashboard
        {{/if}}
      </span>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        {{#each views}}
          {{#if (isDisplayable this)}}
            <li>
              <a id="menu-{{id}}" href="#">{{name}}</a>
            </li>
          {{/if}}
        {{/each}}
        <li class="dropdown minimize">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">... <span class="caret"></span></a>
          <ul class="dropdown-menu">
          </ul>
        </li>
        {{#if authEnabled}}
          {{#if userLogged}}
            <li class="dropdown auth">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                Account: {{capitalize user.name}}
                <span class="caret"></span>
                <ul class="dropdown-menu">
                  <li>
                    <a id="menu-logout" href="#">Logout</a>
                  </li>
                </ul>
              </a>
            </li>
          {{else}}
            <li class="auth">
              <a id="menu-login" href="#">Login</a>
            </li>
          {{/if}}
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
