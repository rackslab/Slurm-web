<div class="container-fluid">
  <div id="jobsmap" class="main">
    <h1 class="page-header">
      Jobs Map
    </h1>
    {{#each racks}}
      <canvas id="cv_rackmap_{{@key}}" width="{{../canvas.canvasWidth}}" height="{{../canvas.canvasHeight}}" />
    {{/each}}
    <canvas id="cv_rackmap_legend" class="canvas-legend" width="{{../canvas.canvasLegendWidth}}" height="{{../canvas.canvasLegendHeight}}"/>
  </div>
</div>
