---
actions:
  view-stats: View general cluster statistics
  view-jobs: View all users jobs
  view-nodes: View all nodes
  view-partitions: View all partitions
  view-qos: View all defined QOS
  view-accounts: View all accounts
  view-reservations: View all reservations
